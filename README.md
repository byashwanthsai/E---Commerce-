# E---Commerce-
A Java-based CLI E-Commerce Order Processing System that manages cart operations, applies discounts and GST, and generates invoices. It reads product and coupon data from CSV files, uses OOP principles, Java Streams, collections, and design patterns like Factory and Decorator, with robust exception handling.
